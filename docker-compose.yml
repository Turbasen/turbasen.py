version: '2'

services:
  dev:
    build: .
    image: turbasen.py
    working_dir: /app
    volumes:
    - .:/app
    env_file: .env
    environment:
      PYTHONUNBUFFERED: 1
      ENDPOINT_URL: https://dev.nasjonalturbase.no
